import { Config } from '../types';

export default {
  action: 'start',
  docsPath: 'docs',
  open: false,
  output: 'html',
  outputPath: 'dist/docs',
  port: 6001,
  readme: true,
  serve: false
} as Config;
